.side-groups
  - current_user.groups.each do |group|
    .side-group
      = link_to group_messages_path(group) do
        .side-group__name
          = group.name
        .side-group__message
          メッセージはありません。



